# frozen_string_literal: true

source 'http://rubygems.org'

gem 'config'
gem 'grape'
gem 'rake'

group :test do
    gem 'byebug'
    gem 'colorize'
    gem 'minitest'
    gem 'minitest-reporters'
    gem 'mocha'
    gem 'rack-test'
    gem 'simplecov'
    gem 'webmock'
end
